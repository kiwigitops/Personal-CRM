# ADR 0003: Self-Hosted Auth

## Status

Accepted

## Decision

Implement email/password auth, password reset, refresh-token sessions, workspace memberships, invitations, and RBAC directly in the API.

## Consequences

The MVP stays self-hostable and can later add OAuth, magic links, and MFA without changing the workspace authorization model.

