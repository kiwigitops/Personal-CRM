# Next-Step Backlog

Highest-priority production follow-ups:

- Add refresh-token rotation and frontend automatic refresh.
- Add full integration tests with ephemeral Postgres and Redis.
- Add attachment upload through MinIO with signed URLs.
- Expand RBAC policy coverage and member removal flows.
- Add CSV upload file picker and conflict resolution UI.
- Add push notification providers for Expo and desktop notifications plugin wiring.
- Add Helm chart values, ingress, secret templates, and autoscaling.
- Add full-text search indexes or vector search for memory entries.

